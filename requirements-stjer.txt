# ═══════════════════════════════════════════════════════════════════════════
#  STJER - Cosecha y busqueda local de jurisprudencia de Entre Rios
#  Instalar con: pip install -r requirements-stjer.txt
# ═══════════════════════════════════════════════════════════════════════════
#
# El buscador local (scripts/stjer.py buscar|voces|estado) NO necesita nada
# fuera de la biblioteca estandar: usa sqlite3 + FTS5 + difflib.
#
# Estas dependencias son para la COSECHA (traer los datos del sitio).

# Parseo de HTML. beautifulsoup4 ya esta en requirements.txt; lxml es el
# parser rapido que usa por debajo.
lxml==5.2.2
beautifulsoup4==4.12.2

# Cliente HTTP de las ramas A/B (el camino rapido) y descarga de PDFs.
# requests ya esta en requirements.txt; se repite para poder instalar este
# archivo solo, sin el resto del proyecto.
requests==2.31.0

# Rama C: solo hace falta si la Fase 0 determina que el POST de Toba no se
# puede replicar. Instalar aparte con:
#     pip install playwright==1.45.0 && playwright install chromium
# playwright==1.45.0
